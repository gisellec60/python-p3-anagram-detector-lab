class Anagram:
    def __init__(self, word):
        self.word = word

    def match(self, list):
        print (list, self.word)
        




listen = Anagram("listen")
listen.match(['enlists', 'google', 'inlets', 'banana'])e